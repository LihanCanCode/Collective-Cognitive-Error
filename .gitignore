# Experiment output — append-only trial logs, regenerated by the runner
results/

# Per-model calibrated banks. Regenerable from the seed + model, and mock runs produce noise.
# NOTE: the final calibrated banks ARE the experimental stimulus and must be committed or
# archived before submission — reviewers cannot reproduce the study without them.
data/calibrated/

# Python
__pycache__/
*.pyc
.pytest_cache/
.venv/
*.egg-info/

# Published papers. Deliberately excluded: these are copyrighted third-party PDFs and this is a
# public repo. Citations live in RESEARCH_PLAN.md; the PDFs stay local.
*.pdf
